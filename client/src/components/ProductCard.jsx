import{Link}from'react-router-dom';import{ShoppingBag,Star}from'lucide-react';import{useCart}from'../context/CartContext';export default function ProductCard({p}){const{add}=useCart();return <article className="card"><Link to={`/product/${p.id}`} className="pic"><img src={p.image}/>{p.badge&&<span>{p.badge}</span>}</Link><div className="cardbody"><div className="rating"><Star size={14} fill="currentColor"/> {p.rating}</div><Link to={`/product/${p.id}`}><h3>{p.name}</h3></Link><p>{p.description}</p><div className="cardfoot"><strong>${Number(p.price).toFixed(2)}</strong><button className="iconbtn" onClick={()=>add(p)} title="Add to cart"><ShoppingBag size={18}/></button></div></div></article>}
