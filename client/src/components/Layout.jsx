import{Link,useNavigate}from'react-router-dom';import{ShoppingBag,UserRound,LogOut,Menu,X,LayoutDashboard}from'lucide-react';import{useState}from'react';import{useAuth}from'../context/AuthContext';import{useCart}from'../context/CartContext';
export default function Layout({children}){const{user,logout}=useAuth();const{count}=useCart();const nav=useNavigate();const[open,setOpen]=useState(false);return <div className="app"><header><Link className="brand" to="/"><span className="logo">N</span>NOVACART</Link><button className="mobile" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?'open':''}><Link to="/shop" onClick={()=>setOpen(false)}>Shop</Link><Link to="/orders" onClick={()=>setOpen(false)}>Orders</Link>{user?.role==='admin'&&<Link to="/admin" onClick={()=>setOpen(false)}><LayoutDashboard size={16}/> Admin</Link>}<span className="navline"/><Link className="cart-link" to="/cart"><ShoppingBag size={18}/> Cart <b>{count}</b></Link>{user?<button className="userbtn" onClick={()=>{logout();nav('/')}}><UserRound size={16}/>{user.name.split(' ')[0]}<LogOut size={15}/></button>:<Link className="login-link" to="/login">Sign in</Link>}</nav></header><main>{children}</main><footer><div><div className="brand"><span className="logo">N</span>NOVACART</div><p>Thoughtful products. Clean design. Better everyday.</p></div><div><strong>Portfolio Project</strong><p>React · Node.js · Express · MySQL</p></div><div><strong>Built to demonstrate</strong><p>Auth · REST API · CRUD · Orders</p></div></footer></div>}
