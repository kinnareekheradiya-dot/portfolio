import{Search,SlidersHorizontal}from'lucide-react';import{useEffect,useState}from'react';import{api}from'../api';import ProductCard from'../components/ProductCard';export default function Shop(){const[p,setP]=useState([]),[q,setQ]=useState(''),[cat,setCat]=useState(''),[sort,setSort]=useState('latest');useEffect(()=>{const t=setTimeout(()=>api(`/products?search=${encodeURIComponent(q)}&category=${cat}&sort=${sort}`).then(setP),250);return()=>clearTimeout(t)},[q,cat,sort]);return <section className="section shop"><div className="pageintro"><span className="eyebrow">SHOP</span><h1>Find your next favorite.</h1><p>Search the collection or explore by category.</p></div><div className="filters"><div className="search"><Search size={18}/><input placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/></div><select value={cat} onChange={e=>setCat(e.target.value)}><option value="">All categories</option><option>Audio</option><option>Wearables</option><option>Lifestyle</option><option>Workspace</option></select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="latest">Latest</option><option value="rating">Top rated</option><option value="price-low">Price: low</option><option value="price-high">Price: high</option></select></div><div className="results"><span><SlidersHorizontal size={15}/> {p.length} products</span></div><div className="grid">{p.map(x=><ProductCard key={x.id} p={x}/>)}</div>{!p.length&&<div className="empty">No products match your search.</div>}</section>}
