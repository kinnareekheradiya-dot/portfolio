import{Link,useLocation,useNavigate}from'react-router-dom';import{useState}from'react';import{useAuth}from'../context/AuthContext';export default function Auth({mode='login'}){const login=mode==='login',[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),{login:signIn,register}=useAuth(),nav=useNavigate(),loc=useLocation();async function submit(e){e.preventDefault();setError('');try{login?await signIn(email,password):await register(name,email,password);nav(loc.state?.from||'/')}catch(e){setError(e.message)}}return <section className="authpage"><div className="authbox"><span className="logo big">N</span><span className="eyebrow">WELCOME TO NOVACART</span><h1>{login?'Welcome back.':'Create your account.'}</h1><p>{login?'Sign in to continue your shopping journey.':'Join the collection and track every order.'}</p><form onSubmit={submit}>{!login&&<label>Name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength="6" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label>{error&&<div className="error">{error}</div>}<button className="primary full">{login?'Sign in':'Create account'}</button></form><div className="switch">{login?<>New here? <Link to="/register">Create an account</Link></>:<>Already have an account? <Link to="/login">Sign in</Link></>}</div></div></section>}
